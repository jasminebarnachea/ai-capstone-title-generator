import { z } from "zod";
export const generatorSchema = z.object({ program:z.string().min(1), category:z.string().min(1), targetUsers:z.string().min(1), region:z.string().max(100).optional(), province:z.string().max(100).optional(), city:z.string().max(100).optional(), barangay:z.string().max(100).optional(), organization:z.string().max(160).optional(), problem:z.string().min(15,"Please describe the problem in at least 15 characters.").max(3000), skills:z.array(z.string()).min(1,"Select at least one skill."), teamSize:z.string().min(1), timeline:z.string().min(1), budget:z.string().min(1) });
export const ideaSchema = z.object({ id:z.string().optional(), title:z.string(), description:z.string(), problemAddressed:z.string(), targetUsers:z.string(), technologies:z.array(z.string()), features:z.array(z.string()), difficulty:z.enum(["Beginner","Intermediate","Advanced"]), timeline:z.string(), budget:z.string(), feasibilityScore:z.number().min(0).max(100), innovationScore:z.number().min(0).max(100), originalityEstimate:z.number().min(0).max(100), scopeScore:z.number().min(0).max(100), objectives:z.array(z.string()).optional(), scope:z.string().optional(), recommendations:z.array(z.string()).optional() });
export const generationSchema = z.object({ ideas:z.array(ideaSchema).length(5), note:z.string().optional() });
export const analysisSchema = z.object({ overallScore:z.number().min(0).max(100), strengths:z.array(z.string()), weaknesses:z.array(z.string()), risks:z.array(z.string()), recommendations:z.array(z.string()), summary:z.string() });
