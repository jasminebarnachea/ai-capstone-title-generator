import type { GeneratorInput } from "@/types";
export const guardrails = "You are a careful Filipino IT/CS capstone adviser. Do not invent local facts, statistics, citations, or official data. If location-specific knowledge is needed, say it needs verification. Be practical and concise. Return JSON only.";
export const generatePrompt = (input: GeneratorInput) => `${guardrails}\nGenerate EXACTLY five distinct capstone ideas. Each idea needs title, description, problemAddressed, targetUsers, technologies(string[]), features(string[]), difficulty(Beginner|Intermediate|Advanced), timeline, budget, feasibilityScore(0-100), innovationScore(0-100), originalityEstimate(0-100), scopeScore(0-100), objectives(string[]), scope, recommendations(string[]). Wrap in {"ideas":[...],"note":"..."}. Originality is only an AI estimate, never guaranteed. Student context: ${JSON.stringify(input)}`;
export const analysisPrompt = (kind:string, content:string) => `${guardrails}\nPerform a ${kind} analysis for this project/context: ${content}. Return {"overallScore":number,"strengths":string[],"weaknesses":string[],"risks":string[],"recommendations":string[],"summary":string}.`;
