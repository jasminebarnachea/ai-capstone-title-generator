"use client"; import Link from "next/link"; import { Menu, Sparkles } from "lucide-react"; import { useState } from "react";
const links=[["Home","/"],["Generate","/generate"],["Features","/features"],["My Projects","/projects"],["About","/about"]];
export function Header(){const [open,setOpen]=useState(false);return <header className="sticky top-0 z-20 border-b border-slate-200 bg-white/95 backdrop-blur"><div className="container flex min-h-16 items-center justify-between"><Link href="/" className="flex items-center gap-2 font-bold text-ink"><span className="grid h-9 w-9 place-items-center rounded-xl bg-brand-600 text-white"><Sparkles size={18}/></span>CapstoneForge <span className="text-brand-600">AI</span></Link><nav className="hidden items-center gap-5 text-sm font-medium md:flex">{links.map(([n,h])=><Link key={h} href={h} className="hover:text-brand-600">{n}</Link>)}</nav><button aria-label="Open navigation" onClick={()=>setOpen(!open)} className="md:hidden"><Menu/></button></div>{open&&<nav className="border-t bg-white p-4 md:hidden">{links.map(([n,h])=><Link key={h} href={h} className="block p-2" onClick={()=>setOpen(false)}>{n}</Link>)}</nav>}</header>}
